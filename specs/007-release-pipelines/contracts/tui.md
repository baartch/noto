# TUI Contract: Update Notice

When a newer version is available, display a non-blocking notice using existing TUI notification surface.

Example copy:

- Title: "Update available"
- Body: "v1.2.4 is available (current v1.2.3)."
- Action: show release URL (if supported) or print to status/log.

No notice when current version is latest or update check fails.