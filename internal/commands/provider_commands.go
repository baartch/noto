package commands

// RegisterProviderCommands registers provider-related commands into r.
// (No provider-scoped slash commands remain.)
func RegisterProviderCommands(_ *Registry) error {
	return nil
}
