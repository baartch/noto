package tui

// This file centralizes shared TUI component composition helpers.
// It exists to keep Bubbles-first composition discoverable in one place.

// useBubblesListForPicker documents the standard list primitive used by pickers.
func useBubblesListForPicker() bool { return true }
